<% if @product.auction.top_bid.user_id == current_user.id %>
  <p>You won the auction!</p>
  <p>
    <%= link_to "Claim this product", transfer_product_path(@product), class: "btn btn-primary", method: :put %>
  </p>
<% else %>
  <p>You lost the auction.</p>
<% end %>